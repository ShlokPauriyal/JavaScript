"use strict" // treat all JS code as newer version

// alert(2+3); // we are using nodejs, not browser

console.log(typeof null); // null datatype is OBJEECT
console.log(typeof undefined); // datatype -> undefined
